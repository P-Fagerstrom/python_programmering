# Create a variable called target age(snakecase) with the value = 100

# Create a variable called current age(snakecase) with you current
#  age as the value

# Create a variable called years to target age(snakecase) with value = 0

# Calculate how many years it will take until you reach the target age
#  and assign it to years to target age

# Print years to target age